phpunit:
    image: phpunit/phpunit
    volumes:
        - .:/app

phpcs:
    image: texthtml/phpcs
    volumes:
        - .:/scripts

composer:
    image: composer/composer
    volumes:
        - .:/app
